[project]
name = "tasks4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.7.1",
    "python-dotenv>=1.2.1",
]

[project.scripts]
tasks4 = "tasks4.main:main"

[tool.uv]
package = true
